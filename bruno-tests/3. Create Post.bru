meta {
  name: Create Post
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/posts/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "content": "This is the content of my first post",
    "published": true
  }
}

tests {
  test("Status is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
  
  test("Response has post data", function() {
    expect(res.getBody()).to.have.property('id');
    expect(res.getBody()).to.have.property('content');
    expect(res.getBody()).to.have.property('author_id');
  });
}